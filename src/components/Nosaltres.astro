---
import type { nosaltresT } from '../lib/types';

interface Props {
  nosaltres: nosaltresT[];
}
const { nosaltres } = Astro.props;
---

<section id='nosaltres' class='bg-zinc-800 px-20 py-16 rounded-xl'>
  <h2 class='text-5xl py-4'>Sobre nosaltres</h2>
  <p class='text-xl font-extralight py-4'>
    Lorem ipsum dolor sit, amet consectetur adipisicing elit. Iusto animi
    excepturi accusamus vitae. Ad optio voluptatem doloribus tempora ducimus
    harum eligendi, provident facilis quas reprehenderit, voluptates, dicta
    facere molestiae molestias. Lorem ipsum, dolor sit amet consectetur
    adipisicing elit. Libero, magni cupiditate praesentium asperiores iusto
    possimus doloribus et, officia eius hic, nemo similique!
  </p>
  <div class='grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-12 p-12'>
    {
      nosaltres.map(({ src, alt, nom }, index) => (
        <div class='flex flex-col gap-6 justify-center'>
          <p
            class={`${
              index % 2 === 0 ? 'xl:order-1' : 'xl:order-0'
            } text-md font-semibold text-center`}>
            {nom}
          </p>
          <img class='w-52 mx-auto' src={src} alt={alt} title={nom} />
        </div>
      ))
    }
  </div>
</section>
