---
import type { capssaleraT } from '../lib/types/contingutTypes.ts';

interface Props {
  capssalera: capssaleraT[];
}
const { capssalera } = Astro.props;
const { imatge, alt } = capssalera[0];
---

<section
  class='bg-zinc-800 flex flex-col md:flex-row p-20 gap-3 text-center md:text-left rounded-xl'>
  <div>
    <h2 class='text-5xl font-bold pb-2'>Protect your password</h2>
    <p class='text-2xl py-4'>
      Lorem ipsum dolor sit amet consectetur adipisicing elit. Assumenda, optio.
    </p>
    <button class='bg-indigo-500 px-3 py-2 hover:bg-indigo-400 rounded-md'
      >Comencem</button
    >
  </div>
  <img class='w-80 mx-auto' src={imatge} alt={alt} />
</section>
